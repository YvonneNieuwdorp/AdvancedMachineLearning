# Practical assignment 4 - group 3

This project classifies tweets from multiple celebrities using TF-IDF + Logistic Regression and Naive Bayes.

## Requirements
- Python 3.10+
- Libraries listed in requirements.txt

## How to run
1. Install required libraries: `pip install -r requirements.txt`
2. Run the main pipeline: `python main.py`