FEEDBACK Week 1

 - Work in a `.py` file
 - Use functions
 - Code must be repeatable


DIVISION OF LABOUR

Yvonne
Contributions:
 - Implemented `def clean_books(input_dir, output_dir)` to remove Project Gutenberg boilerplate and clean the raw text files.
 - Implemented `def train_and_tokenize(clean_dir, model_output_dir, vocab_size=10000)` to train a BPE tokenizer and save both the tokenizer and tokenized outputs.

AI usage:
 - Used AI to add and standardize function comments.
 - Asked AI for help when encountering logical issues (e.g., correctly determining the index range for removing the table of contents).
 - Requested conceptual guidance when getting stuck, then implemented the suggested ideas independently in the final code.


Jessie
Contributions:
 - Implemented load_tokens(books), lemma_corpus(tokens) and stem_corpus(tokens).
 - Calculated and analyzed the difference between the number of unique lemmas and stems.
 - Structured the script into a clear and repeatable NLP preprocessing pipeline.
 - Polished the overall script to improve readability and consistency.

AI usage:
 - Used AI to add and standardize function comments.
 - Requested minor guidance on improving code structure and readability, which was then manually implemented and adjusted.
